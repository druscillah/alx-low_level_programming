#include <stdio.h>
#include "main.h"

/**
 * main - print the name of the program
 * @argc: count argumets
 * @argv: arguments
 * Return: always 0 (success)
 */
int main(int argc__attribute__((unused))
	       	char *argv[])
{
	printf("%s\n", argv[0]);
	return (o);
}
