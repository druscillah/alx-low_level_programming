#include <stdio.h>

/**
 * main - Reverse of the alphabet lowercase
 * Return: Always 0
 */
int main(void)
{
	char la;

	for (le = 'z'; le >= 'a'; le--)
		putchar(le);

	putchar('\n');

	return (0);
}
